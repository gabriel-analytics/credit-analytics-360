name: credit_analytics_360
version: '1.0.0'
config-version: 2

profile: financeflow

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  credit_analytics_360:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

vars:
  data_path: "../../gen/data"
  start_date: '2023-01-01'
  end_date: '2024-12-31'
